export type FV = {
  location: string;
  photoNo: string;
  sizes: { width: string; height: string }[];
};

export type EditFV = {
  location: string;
  sizes: { width: string; height: string }[];
};
